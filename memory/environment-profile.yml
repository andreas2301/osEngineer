# Environment Profile
# Auto-generated by /osEngineer:init during discovery protocol.
# NEVER assume — always ask the user to confirm.

# Detected execution context
detected_at: ""
detected_by: "osEngineer:init"

# Environment type: terminal_server | ide | web | autonomous_daemon
# The user MUST confirm this classification.
type: ""

# Confidence: low | medium | high
confidence: "low"

# Detected signals (filled by heuristics)
signals:
  tty: null           # true if stdin is a tty
  ssh_client: null    # value of $SSH_CLIENT
  display: null       # value of $DISPLAY
  ide_markers: []     # e.g., [".vscode", ".cursor", ".idea"]
  ci_markers: []      # e.g., ["CI", "GITHUB_ACTIONS", "JENKINS_URL"]
  daemon_markers: []  # e.g., ["systemd", "supervisor"]

# User-confirmed adaptations
adaptations:
  interactive: null   # true if user is present for HITL
  approval_required: null  # true for terminal_server/ide, false for daemon
  verbose_logging: null    # true for debugging, false for daemon
  auto_commit: null        # false for terminal_server/ide, true for daemon
  notify_on_complete: null # true for daemon, false for interactive

# Live system access (filled if /opt/sovereign-shield/ is reachable)
live_system:
  reachable: false
  path: "/opt/sovereign-shield"
  docker_access: false
  vault_access: false
  user: "engineer"

# Validation
# This file is considered stale if detected_at is older than 7 days.
# Re-run /osEngineer:init to refresh.
